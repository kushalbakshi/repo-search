# RepoSearch Environment Variables

# Required: OpenAI API key for generating embeddings
OPENAI_API_KEY=your_openai_api_key_here

# Optional: GitHub token for accessing GitHub API (increases rate limits)
# GITHUB_TOKEN=your_github_token_here

# Optional: Path to the data directory where the vector database will be stored
# DATA_DIR=data

# Optional: Embedding model name to use
# EMBEDDING_MODEL=text-embedding-3-small

# Optional: Number of texts to embed in a single batch
# EMBEDDING_BATCH_SIZE=16

# Optional: Maximum number of tokens per chunk
# CHUNK_SIZE=1000

# Optional: Number of tokens to overlap between chunks
# CHUNK_OVERLAP=100

# Optional: Maximum number of search results to return
# MAX_RESULTS=10

# Optional: Minimum similarity score for search results
# SCORE_THRESHOLD=0.0
